import React from 'react';
import { PEOPLE_URL } from '@/constants';

interface CampProps {
    backgroundImage: string;
    title: string;
    subtitle: string;
    peopleJoined: string;
};

const CampSite = () => {
    return (
        <>

        </>
    );
};

const Camp = () => {
    return (
        <>
            
        </>
    );
};

export default Camp;